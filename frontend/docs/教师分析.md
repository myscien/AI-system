        Student(student_no="20300101", student_name="A", grade="6", class_name="1", status="active"),
        Student(student_no="20300102", student_name="B", grade="6", class_name="1", status="active"),
        Student(student_no="20300103", student_name="C", grade="6", class_name="2", status="active"),
        Student(student_no="20300104", student_name="D", grade="7", class_name="1", status="active"),
    ])
    db.add_all([
        AnswerRecord(task_id=task.id, question_pk=q1.id, student_no="20300101", student_name="A", session_id="s1", student_answer_json="[]", correct_answer_json="[]", is_correct=1),
        AnswerRecord(task_id=task.id, question_pk=q2.id, student_no="20300103", student_name="C", session_id="s1", student_answer_json="[]", correct_answer_json="[]", is_correct=0),
        AnswerRecord(task_id=task.id, question_pk=q1.id, student_no="20300104", student_name="D", session_id="s1", student_answer_json="[]", correct_answer_json="[]", is_correct=1),
    ])
    db.commit()

    result = build_task_analysis(task.id, db, grade="6", class_name="1")
    print(result.model_dump())
'@ | .\.venv\Scripts\python.exe -
前端需要知道这些改法：
const params = new URLSearchParams()

if (grade) params.set("grade", grade)        // 例如 "6"
if (className) params.set("class_name", className) // 例如 "1"

const res = await fetch(`/api/tasks/${taskId}/analysis?${params.toString()}`)
const data = await res.json()
返回里的 student_progress 现在每个学生多了两个字段：
{
  "student_no": "20300101",
  "student_name": "张三",
  "grade": "6",
  "class_name": "1",
  "answered_count": 1,
  "correct_count": 1,
  "accuracy": 1.0,
  "progress": 0.5,
  "status": "in_progress",
  "is_low_accuracy": false
}
统计口径也改了：
{'task_id': 1, 'task_title': 'test', 'total_students': 2, 'assigned_students': 2, 'submitted_students': 1, 'completed_students': 0, 'completion_rate': 0.0, 'average_accuracy': 1.0, 'low_accuracy_student_count': 0, 'question_stats': [{'question_id': 1, 'question_pk': 1, 'total_answers': 1, 'correct_count': 1, 'wrong_count': 0, 'accuracy': 1.0}, {'question_id': 2, 'question_pk': 2, 'total_answers': 0, 'correct_count': 0, 'wrong_count': 0, 'accuracy': 0.0}], 'student_progress': [{'student_no': '203
… 158 chars omitted, open in editor for full output …
ss', 'is_low_accuracy': False}, {'student_no': '20300102', 'student_name': 'B', 'grade': '6', 'class_name': '1?', 'answered_count': 0, 'correct_count': 0, 'accuracy': 0.0, 'progress': 0.0, 'status': 'not_started', 'is_low_accuracy': False}], 'lowest_accuracy_questions': [{'question_id': 2, 'question_pk': 2, 'total_answers': 0, 'correct_count': 0, 'wrong_count': 0, 'accuracy': 0.0}, {'question_id': 1, 'question_pk': 1, 'total_answers': 1, 'correct_count': 1, 'wrong_count': 0, 'accuracy': 1.0}]}

•
不传 grade 时，默认使用任务自己的年级 task.grade
•
传 grade 时，按传入年级过滤
•
传 class_name 时，在年级基础上继续按班级过滤
•
assigned_students：当前筛选范围内的 active 学生人数
•
submitted_students：当前筛选范围内有答题记录的学生人数
•
student_progress：现在包含未开始答题的学生，状态为 not_started
•
question_stats：只统计当前筛选范围内学生的答题记录
•
total_students：现在等于当前筛选范围内学生总数，也就是 assigned_students
我已验证：
compileall 通过
main.py import 通过
内存数据库测试通过：grade=6&class_name=1 不会统计到其他班级或其他年级
注意：class_name 要和导入学生名单时存储的班级文本完全一致，比如都是 1。
