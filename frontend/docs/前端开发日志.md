# 前端开发日志

## 2026-04-24

### 任务 1：学生学号入口与任务列表

本次完成内容：

- 首页改为学生入口页
- 增加 8 位学号校验
- 支持按学号查询任务列表
- 对齐学生任务接口：
  - `GET /api/student/tasks?student_id=...`
- 兼容任务列表返回结构：
  - 数组
  - 或 `{ student_id, tasks }`
- 兼容任务项主键字段 `task_id -> id`

验证：

- 执行 `npm.cmd run build`
- 构建通过

### 任务 2：答题页改单题实时提交

本次完成内容：

- 答题页进入时生成 `sessionId`
- `sessionId` 优先使用 `crypto.randomUUID()`
- 新增 `submittedQuestionMap`
- 新增 `savingQuestionMap`
- 新增 `saveErrorMap`
- 新增单题实时提交 API：
  - `POST /api/tasks/{taskId}/questions/{questionId}/submit`
- 同题在同一页面会话内只首次保存
- 刷新后重新进入任务生成新 `sessionId`
- 答题页底部原“提交全部答案”按钮已隐藏

验证：

- 执行 `npm.cmd run build`
- 构建通过

### 任务 3：新增教师基础学情分析页

本次完成内容：

- 新增教师分析页
- 显示参与学生数、题目正确率表、学生完成情况表、薄弱题目区
- 新增分析接口调用：
  - `GET /api/tasks/{taskId}/analysis`
- 正确率统一按百分比展示
- 若后端未返回 `lowest_accuracy_questions`，则前端从 `question_stats` 中计算

验证：

- 执行 `npm.cmd run build`
- 构建通过

### 任务 4：引入 Vue Router 管理页面

本次完成内容：

- 引入正式 Vue Router 路由管理
- 新增 `src/router/index.js`
- `main.js` 注册 router
- `App.vue` 收敛为 `RouterView`
- 新增页面：
  - `src/pages/HomePage.vue`
  - `src/pages/PracticePage.vue`
  - `src/pages/TeacherAnalysisPage.vue`
- 新增路由：
  - `/`
  - `/practice/:taskId`
  - `/teacher/analysis/:taskId`
- 学生点击任务卡片后跳转到：
  - `/practice/{taskId}?student_no={studentNo}`

验证：

- 执行 `npm.cmd run build`
- 构建通过

### 任务 5：教师任务管理与题目导入页面

本次完成内容：

- 新增教师任务管理页：
  - `/teacher/tasks`
- 新增教师任务列表展示
- 新增创建任务表单：
  - `title`
  - `grade`
  - `description`
- 新增题目 JSON 导入表单：
  - `taskId`
  - `jsonText`
- 导入前前端校验：
  - 非空
  - 合法 JSON
  - 包含非空 `questions` 数组
- 校验失败时不请求后端
- 导入成功后：
  - 显示成功提示
  - 刷新任务列表
  - 跳转到对应分析页
- 任务列表操作：
  - 导入题目
  - 查看分析
- 首页新增教师任务管理入口
- 新增教师接口调用：
  - `GET /api/teacher/tasks`
  - `POST /api/teacher/tasks`
  - `POST /api/teacher/tasks/{taskId}/questions/import`

验证：

- 执行 `npm.cmd run build`
- 构建通过

### 任务 6：优化首页教师入口跳转逻辑

任务来源：

- `docs/前端当前任务.md`

本次完成内容：

- 去掉首页单独的教师分析直达入口
- 首页只保留“进入教师任务管理”
- 教师统一先从 `/teacher/tasks` 进入
- 教师分析页路由与页面保留不变
- 教师继续从任务管理页跳转到分析页

涉及文件：

- `docs/前端当前任务.md`
- `src/pages/HomePage.vue`
- `docs/05_前端页面与状态逻辑.md`
- `docs/前端开发日志.md`

验证：

- 执行 `npm.cmd run build`
- 构建通过

影响说明：

- 未修改后端代码
- 未修改学生答题逻辑
- 未新增路由
